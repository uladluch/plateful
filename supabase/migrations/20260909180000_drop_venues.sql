-- Точки сетей больше не наши: экран «рядом» спрашивает карту Apple по
-- имени каждой сети из пака.
--
-- Своя таблица прожила день. Она честно отвечала на «что рядом» для пяти
-- сетей с часами работы в строке — но каждая следующая сеть требовала
-- своего импортёра с тех же сайтов, что режут скрейперы, а у Apple все
-- девяносто шесть сетей есть уже сегодня. Часы теперь в карточке места
-- Apple, в одном нажатии; в строке списка их нет, и это осознанная цена.
--
-- `chains.price_tier` остаётся: полоса цены — свойство сети, не точки.

drop function if exists public.venues_near(double precision, double precision,
                                           double precision, text[], integer);
drop table if exists venues;
