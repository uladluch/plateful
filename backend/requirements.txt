psycopg[binary]>=3.2
python-dotenv>=1.0
# Снимки блюд приводятся к одному квадрату перед загрузкой в Storage.
# Стоял только локально, и тесты матчера падали в CI на импорте.
Pillow>=10.0
pdfplumber>=0.11  # гиды по питанию в PDF: единственный источник для сетей, чей сайт рисует меню скриптом
